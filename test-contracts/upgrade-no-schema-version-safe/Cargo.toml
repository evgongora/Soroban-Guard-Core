[package]
name = "sg-test-upgrade-no-schema-version-safe"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = "=21.0.0"

[dev-dependencies]
soroban-sdk = { version = "=21.0.0", features = ["testutils"] }

[lib]
crate-type = ["cdylib"]
